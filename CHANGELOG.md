# 更新日志

**中文** · [English](CHANGELOG.en.md)

版本按时间倒序记录。v0.1.5 作为下一版本暂存，尚未发布；发布时补充日期，再创建下一版本条目。

## [v0.1.5]：未发布

### 新增

- 增加 Edge TTS 合成与 MP3 播放核心，并由统一的 `arcana-world-tts` 配套程序负责合成和播放；尚未接入 TUI 和自动弹幕播报。
- 播报逐条完成合成和播放，默认最多等待 5 条，满时丢弃新消息；支持取消当前播报、清空队列及停止后继续使用。
- 增加 `make tts`、Nix 语音开发环境和配套包；`make desktop` 与发行包包含语音程序，独立 TUI 仍不依赖音频运行时。

- 登录后自动监听自己的直播间，支持弹幕、礼物、醒目留言（SC）和大航海消息；可浏览本地历史、暂停跟随、分页及切换房间。
- 解析 177 个已注册命令，支持新版礼物、互动和高能榜的 protobuf 消息格式。
- 显示白名单区分常态显示、开启 `f` 后显示和始终隐藏的消息，完整清单见[弹幕显示白名单](docs/danmaku-whitelist.md)。
- 暂停或翻阅历史时，在顶部提示有新消息；按 `Space` 或 `End` 返回最新。
- 提供 Nix Flake 构建、运行入口和检查任务，并锁定构建依赖。
- 使用 `nix develop`（或 `nix develop .#arcana-world`）进入独立的 TUI 开发环境，使用 `nix develop .#arcana-world-overlay` 进入浮层开发环境；Linux 下会自动启用 Wayland 构建标签。开发使用 Go 模块模式，无需生成 `vendor/`。
- 在设置页管理原生浮层，支持运行时启停、外观持久化及房间状态、实时弹幕、组合三种内容模式；命令行可临时覆盖启用状态。
- 提供 `make desktop` 和 Nix 桌面配套包；跨平台发行统一为根目录主程序与 `libexec` 浮层组成的便携包，保留独立 TUI 开发构建。
- 设置页底部新增「恢复默认设置」和「清除应用数据」。恢复默认仅影响设置页配置，保留 OBS 配置和登录凭据；清除须输入 `arcanaworldclear`，关闭后台资源后删除当前数据目录的应用文件及对应登录凭据并退出，输入不匹配则返回设置页。

### 改进

- 封面使用字符预览时显示黄色提示，说明图形预览不可用并推荐支持 Kitty 图形协议的终端；窄窗口自动换行。
- 浮层默认左侧居中、零偏移、字号 16，内容默认为实时弹幕并排在内容选项首位；已有保存值保持不变。

- 消息按时间从上到下排列；跟随时自动滚到底部，「显示其他事件」开关固定在顶部。
- 入场和关注默认显示，分享和特别关注需开启 `f`，互关通知隐藏。当前支持的 30 个 PK 命令均需开启 `f`。
- 隐藏跨房广播、推荐、下播列表和非 PK 榜单等通知，即使开启 `f` 也不会显示；原始业务消息仍保存。
- 历史、原始业务消息及操作日志保留最近七天并自动清理；有可靠标识的消息支持跨重启去重。
- OBS 连接设置集中到独立的「OBS」页，代理和推流协议保留在「设置」页。
- 补充界面截图，精简中英文 README，并将逐项白名单移至 `docs/`。
- 分开聊天状态管理和显示逻辑，统一切房状态重置，删除冗余代码和重复测试。

### 修复

- 清除应用数据时一并删除仍由当前账号 ID 引用的凭据，避免账号索引不完整时遗留登录信息。
- 恢复默认设置时回收旧客户端的空闲连接，并回收退出检查使用的临时连接。
- 修复极窄终端中封面降级提示的双宽字符越界问题。
- 按主程序真实路径定位 `libexec` 中的浮层，支持整体搬迁和通过符号链接启动，不依赖当前工作目录。
- 移除「恢复默认外观」后的多余冒号，增加操作确认和恢复成功提示。
- 修复滚动弹幕时意外暂停跟随的问题，确保新消息及时显示。
- 修复暂停、翻页及异步读取时的阅读位置问题；隐藏通知不再触发新消息提示，提醒不会串到其他房间。
- 修复从历史房间返回直播房间后立即暂停时可能误用旧房间序号的问题。
- 切换账号时立即隐藏旧弹幕监听来源，防止浮层接收旧房间的异步结果。
- 拒绝超出 protobuf 合法范围的字段号，同时兼容合法的未知字段。
- 状态栏长错误信息按终端宽度自动换行，并为多行提示预留显示高度，避免错误详情被截断。
- 退出时不再仅因启用自动推流而尝试连接未使用的 OBS；保留开播及已知推流活动的停止检查，并精简连接失败提示。

### 注意事项

- 断线期间的消息不会在重连后补回；本地历史可能不完整，不能作为完整财务对账依据。
- 能解析的消息不一定展示；未列入白名单或解析失败的消息仍隐藏。

## [v0.1.4]：2026-09-14

### 新增

- 提供中英文界面与提示，支持自动识别系统语言及通过 `--lang` 手动切换。
- 提供 macOS、Windows、Linux 的 x64 / ARM64 发布包，共六种平台组合。
- 提供中英文 README、项目图标、安装链接、OBS 配置说明和快捷键文档。

### 改进

- 将版本发布从常规 CI 拆分为独立工作流，校验版本标签后再构建发布包。
- 发布包统一附带许可证和 SHA-256 校验文件，支持正式版和预发布版本。

## [v0.1.3]：2026-09-13

### 新增

- 提供终端直播控制台，支持扫码登录、多账号保存与切换，以及开播和停播。
- 支持修改直播间标题、分区、公告及封面，并提供封面裁剪和终端预览。
- 支持 OBS WebSocket v5 连接、写入推流配置，以及自动同步开播和停播。
- 支持 RTMP / SRT 和网络代理；账号凭据与 OBS 密码通过系统密钥环保存。
- 提供持久化配置、操作日志，以及 `--config-dir`、`--version` 等命令行选项。
- 建立基础测试和 CI，提供 Linux x64 发布包及校验文件。

[v0.1.5]: https://github.com/TarocchiLive/arcana-world/tree/dev
[v0.1.4]: https://github.com/TarocchiLive/arcana-world/releases/tag/v0.1.4
[v0.1.3]: https://github.com/TarocchiLive/arcana-world/releases/tag/v0.1.3
