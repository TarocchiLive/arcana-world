# 更新日志

**中文** · [English](CHANGELOG.en.md)

版本按时间倒序记录。

## 未发布

### 新增

- 直播状态旁显示观众人数，每 30 秒刷新；读取失败时显示未知。
- 弹幕页新增房管与黑名单子菜单，支持查看名单、任免房管、永久直播间禁言和解除禁言。按用户名或 UID 查找后直接进入确认框；四类操作均在框内展示小头像、名称及 UID，确认后执行并记录操作日志；不修改账号黑名单。
- 弹幕页新增「观众列表」（`u`）和「大航海」（`g`）两个可点击入口，各展示前 20 名。观众列表显示贡献值；大航海列出付费成员，不限当前是否在场。名单显示总督、提督、舰长身份和粉丝牌，匿名成员隐藏 UID；支持刷新与滚动，返回后保留弹幕草稿和阅读位置。
- 单击弹幕说话人的名字打开资料浮框，显示头像、名称、佩戴的粉丝牌和 UID；可禁言 1 小时、加入直播间黑名单、设为房管或打开主页，前三项需再次确认。拖动名字仍选择文字，关闭浮框保留草稿与阅读位置。

### 修复

- 修复 Windows 32 位等 32 位平台上的左边框缺失、输入光标偏移和通知布局错位。
- 确认框头像支持 Kitty/Ghostty 原生图片协议，按终端像素比例显示约四行高的方形缩略图；不支持时回退到字符预览，关闭或切换确认框时清理图片。
- 头像加载失败时仍可核对名称和 UID 后确认操作；人数查询不再占用停播等操作所需的会话锁。
- 修复实时弹幕刷新后，点击历史记录中的名字可能打开错人资料的问题；从资料浮框返回后保留历史内容和阅读位置。
- 同一数据目录已被占用时，启动提示说明目录及处理方法，不再只显示 `timeout`。
- 修复旧历史库中已过期记录缺少原始消息引用时，启动清理失败并阻止弹幕加载的问题；只清理过期记录，保留期内数据不变。
- 修复切回原直播间时旧人数请求覆盖新结果、名单与资料浮框关闭后阅读位置跳动，以及重复确认和取消房管操作后的状态问题。

## [v0.2.2]：2026-09-24

### 修复

- 修复旧历史库中已过期记录缺少原始消息引用时，自动清理失败，导致弹幕历史无法加载、监听无法启动的问题。清理仅移除超过七天的记录，保留期内的事件及原始消息不变。

### 升级提示

- 本版本是 0.2.1 的补丁，不包含 0.3.0 alpha 的新功能。请完整替换发行包，无需删除历史库。
- 已使用 0.3.0 alpha 升级过的数据目录不能直接交给 0.2.2 使用；请继续使用包含同一修复的 alpha 开发版本。

## [v0.3.0-alpha.1]：2026-09-22（预发布）

### 新增

- 弹幕框底部新增自动换行的输入区，按 `/` 或点击编辑，最多 40 字；`Enter` 发送，`Esc` / `Tab` 返回浏览，发送失败保留草稿。
- TTS 页支持设置并保存 0–100 的音量，默认 80，0 为静音；试听和事件播报共用该设置。
- 设置页提供微光、午夜、北境、余烬和纸笺五种 TUI 主题，选择后保存，重启后继续使用。
- 支持鼠标左键切换页面、操作菜单与选择器、定位输入光标、提交表单和确认操作；滚轮可浏览长列表与内容。
- 大窗口顶部显示紧凑的 Arcana / WORLD 字符标题与缓慢流光；移除地球，主页面和子菜单保持一致的顶栏与底栏高度。
- 设置页新增「外观」，可保存缓慢流光、紧凑标题和仅显示警告的偏好。
- 按 `q` 或 `Ctrl+C` 退出前显示确认框，默认取消；取消后保留未提交的输入和原菜单。
- 支持鼠标指针形状协议的终端中，空白区域使用箭头，操作项使用手形，输入区域使用文本指针。
- 鼠标悬浮时高亮可操作选项，不触发操作，也不改变键盘选中项。
- 帮助页增加新手开播、OBS 连接、画面与推流排障、弹幕历史、浮层与语音、键鼠设置六个主题，支持独立阅读和返回目录。
- 弹幕、历史、日志和通知支持正文内拖动选择；按 `y` 复制、`Esc` 清除选区，仅在底栏提示，无复制按钮或自动复制。`Ctrl+C` 仍用于退出确认。

### 改进

- 升级至 Lip Gloss v2、Bubble Tea v2 和 Bubbles v2，重构全部 TUI 页面、表单和选择器。统一圆角细边框、分层底色和焦点样式；宽屏使用分组侧栏，窄屏保留紧凑导航。
- 重构弹幕页：启动载入近 24 小时内最新 10 条，主框默认最多保留 30 条，右侧滚动条显示阅读位置。移除房间切换和历史翻页操作，将监听、附加事件及条数上限放入设置；历史改为按起止时间查询的独立滚动子页面，不影响主框和草稿。
- 历史起止时间输入框支持用上下方向键切换，保留 Tab 和 Enter 操作；设置页将所有开关项集中排列在其他选项之前。
- 修复输入光标在中文、长文本滚动和窗口缩放后错位的问题，改善输入法候选窗定位，覆盖弹幕、表单、标题编辑、分区搜索和历史时间输入。
- 将新手开播指引精简为登录与直播间设置、连接 OBS、开播和停播四步，末尾提供视频教程链接；修复 WORLD 字标中 R 的笔画连接。
- 右上角通知仅展示操作结果和警告，默认 5 秒后关闭，也可手动关闭和滚动。加载进度放在底部状态栏，不再弹出通知；各页面的快捷键提示采用统一布局。
- 确认操作使用居中浮层，淡化并锁定下方页面，默认选中取消；直播页将开播／关播放在刷新之前。
- 精简账号、直播间、OBS 和 TTS 页面说明，明确自动连接、自动推流和弹幕监听的触发时机，功能行为不变。
- 页面名称、开播状态和 OBS 状态移到顶栏右侧，通知显示在其下方。底部仅显示快捷键和操作进度，弹幕页补齐 Tab 切页提示。
- 鼠标悬浮仅使用文字与背景高亮，去掉整行下划线；复用未变化的画面，仅重绘悬浮行，并避免指针形状输出触发重复重绘，降低快速移动时的 CPU 开销。
- 降低 TUI 空闲 CPU 开销：流光仅更新分隔线，无变化的轮询复用画面；终端报告失焦时暂停装饰动画，恢复焦点后继续，后台业务轮询保持运行。
- 修复通知部分遮挡操作项时，鼠标悬浮高亮覆盖通知边框与正文的问题；异步重绘后保持正确的鼠标指针形状。
- 修复浅色主题下拖选时正文区域背景变黑的问题，保持未选中文字和背景的原有颜色。

### 升级提示

- 这是 0.3.0 的首个 alpha 预发布版本，供提前体验和反馈问题。
- 请完整替换发行包中的主程序和 `libexec` 目录。新的音量设置需要配套的 TTS 辅助程序，混用旧程序会导致语音播报失败。

## [v0.2.1]：2026-09-20

### 新增

- Linux 未找到系统密钥环或无法连接用户凭据服务时，自动将登录凭据和 OBS 密码保存到数据目录下的 `credentials/secrets.json`，并提示已回退至文件存储；密钥环锁定或拒绝访问时仍报错。文件以明文保存，Unix 目录和文件权限分别限制为 `0700`、`0600`，Windows 使用受限 ACL；已有凭据文件时继续从文件读写。
- 增加凭据后端选择（含仅内存模式）、代理、OBS 自动连接/推流和 TTS 命令行选项；显式覆盖只对本次运行生效，不写回配置。用法见 `--help`。
- 增加 `--doctor` 只读环境诊断，检查配置、凭据服务与可选辅助程序，不读取凭据内容或启动图形、音频及业务服务。
- 扫码登录和身份验证页面支持按 `o` 在系统图片查看器中打开二维码，不中断登录状态轮询；Windows 10 及更早版本默认自动打开图片，不在终端绘制二维码。
- 浮层支持按显示器名称独立勾选多个屏幕，鼠标点击或键盘切换后立即保存并生效；共用内容快照，断开的已选屏幕不会回退到其他显示器，重连后恢复显示。
- 浮层可分别设置普通文字、背景、舰长、提督、总督和醒目留言的颜色。输入 `#RRGGBB` 时实时预览，确认后保存，取消恢复原色；默认使用纯白正文、珊瑚红大航海、暖金色醒目留言和深灰蓝背景。

### 改进

- 大航海开通消息按时长显示和播报，例如「张三开通了1个月的舰长」，保留年付和短期时长；礼物继续显示赠送数量。
- 浮层文字增加细深色描边，可在「浮层内容与外观」中开关，默认开启；默认文字不透明度为 1.0、背景不透明度为 0.35，提高浅色背景下的可读性。开关立即保存并生效。

### 修复

- 浮层外观编辑保存或取消后返回原选项，保留列表位置，方便连续调整；从外观列表按 Esc 才返回浮层主菜单。
- 修复 macOS 浮层背景覆盖整块屏幕的问题；背景仅在配置的浮层位置和尺寸内绘制，其余区域保持透明。
- 修复 Windows 文件凭据后端申请目录 ACL 权限失败的问题，保持仅当前用户和 SYSTEM 可访问。
- 修复浮层启动时套接字超时与上下文计时器的竞态，启动期限耗尽时统一返回截止时间错误。
- 将最低 Go 版本升级至 1.26.8，使 CI 和发行构建使用包含标准库安全修复的工具链。

### 升级提示

- 请完整解压新发行包，同时更新主程序和 `libexec` 目录，避免混用不同版本。

## [v0.2.0]：2026-09-18

本版本将 `v0.2.0-rc.2` 的应用代码发布为正式版，重新构建所有平台包，程序版本号更新为 `0.2.0`。预发布版本保留供查阅。

### 相比 v0.1.4 的主要变化

- 登录后自动监听直播间，支持弹幕、礼物、醒目留言（SC）和大航海消息，并提供本地历史、暂停跟随、分页及切换房间。
- 提供无焦点、鼠标穿透的原生桌面浮层，支持运行时启停、外观设置及独立的事件选择。
- 接入 Edge TTS 语音合成与 MP3 播放，在独立「TTS」页管理 10 种中文音色、试听、播放队列及播报事件。
- 历史与操作日志保留最近七天并自动清理，支持历史存储自动迁移和跨重启消息去重；修复 Windows 日志文件替换失败的问题。
- 切换或删除账号前确认并停止旧直播及关联 OBS 推流；增加恢复默认设置和清除应用数据入口。
- 统一 Linux、macOS、Windows 浮层的自动换行与溢出行为，保持窗口位置和尺寸不变。
- 发行包包含主程序、浮层和语音程序，覆盖 macOS / Linux 的 amd64、arm64，以及 Windows 的 386、amd64、arm64；提供 SHA-256 校验和与 GitHub 构建来源证明。
- 提供 Nix Flake 构建和开发环境，独立 TUI 仍不依赖图形或音频运行时。

完整变更见 [v0.2.0-rc.1](https://github.com/TarocchiLive/arcana-world/releases/tag/v0.2.0-rc.1) 和 [v0.2.0-rc.2](https://github.com/TarocchiLive/arcana-world/releases/tag/v0.2.0-rc.2)。

### 升级与使用注意事项

- 历史数据库会自动升级。降级应用前请恢复升级前的数据库备份，不要用旧版本直接打开已升级的数据库。
- 断线期间的消息不会在重连后补回，本地历史不能作为完整财务对账依据。
- 浮层内容和播报模板目前仅支持中文；语音合成需要联网。
- Windows 桌面浮层不保证覆盖独占全屏应用，建议使用窗口化或无边框全屏。

## [v0.2.0-rc.2]：2026-09-18（预发布）

### 新增

- Windows 桌面包加入 386 构建、CI 检查与发行产物；32 位浮层使用标准系统调用传递参数，无需汇编或 CGO。

### 改进

- 将 10 种 TTS 音色的选择入口从「设置」页移至「TTS」页，与启停、试听、停止播放和事件选择集中管理。
- Windows 64 位浮层使用 purego 类型化绑定替换自有 ABI 调用桥和汇编，保持无 CGO 构建及原有绘制行为。
- 更新 Go 依赖，其中 Bubbles 升级至 1.0.0，Oto 升级至 3.5.0。

### 修复

- 统一 Linux、macOS、Windows 浮层的换行与溢出行为：所有事件按宽度自动换行，以配置字体的固定行高显示末尾能容纳的完整行，底部余高留空，避免最新消息移出可视区域或顶部露出半行；浮层位置、尺寸和已有高度配置不变。
- 修复 Windows 操作日志清理过期记录时替换已打开文件失败的问题；替换后继续写入同一日志路径。

## [v0.2.0-rc.1]：2026-09-17（预发布）

### 新增

- 将 Edge TTS 合成与 MP3 播放接入 TUI，使用独立「TTS」页启停、试听及清空播放队列；设置页提供 10 种中文音色。
- 「弹幕浮层」与「TTS」独立成页，各自勾选 23 类默认可见事件，缺省全选；代码内分别声明中文浮层模板和播报模板。TTS 仅播报新事件，切换账号、房间、音色或停止监听时取消旧播报。
- 播报逐条完成合成和播放，默认最多等待 5 条，满时丢弃新消息；支持取消当前播报、清空队列及停止后继续使用。
- 增加 `make tts`、Nix 语音开发环境和配套包；`make desktop` 与发行包包含语音程序，独立 TUI 仍不依赖音频运行时。

- 登录后自动监听自己的直播间，支持弹幕、礼物、醒目留言（SC）和大航海消息；可浏览本地历史、暂停跟随、分页及切换房间。
- 解析 177 个已注册命令，支持新版礼物、互动和高能榜的 protobuf 消息格式。
- 显示白名单区分常态显示、开启 `f` 后显示和始终隐藏的消息，完整清单见[弹幕显示白名单](docs/danmaku-whitelist.md)。
- 暂停或翻阅历史时，在顶部提示有新消息；按 `Space` 或 `End` 返回最新。
- 提供 Nix Flake 构建、运行入口和检查任务，并锁定构建依赖。
- 使用 `nix develop`（或 `nix develop .#arcana-world`）进入独立的 TUI 开发环境，使用 `nix develop .#arcana-world-overlay` 进入浮层开发环境；Linux 下会自动启用 Wayland 构建标签。开发使用 Go 模块模式，无需生成 `vendor/`。
- 在独立「弹幕浮层」页管理原生浮层，支持运行时启停、外观持久化及房间状态、实时事件、组合三种内容模式；命令行可临时覆盖启用状态。
- 提供 `make desktop` 和 Nix 桌面配套包；跨平台发行统一为根目录主程序与 `libexec` 浮层组成的便携包，保留独立 TUI 开发构建。
- 设置页底部新增「恢复默认设置」和「清除应用数据」。恢复默认重置应用偏好、浮层、TTS 音色及事件选择，保留 OBS 配置和登录凭据；清除须输入 `arcanaworldclear`，关闭后台资源后删除当前数据目录的应用文件及对应登录凭据并退出，输入不匹配则返回设置页。

### 改进

- 补齐浮层、TTS、事件选项、音色名称和运行状态的中英文界面翻译；实际浮层内容和播报模板仍使用中文。
- OBS 自动连接和自动推流对新配置默认开启；已有配置中显式保存的关闭值保持不变。
- 浮层与 TTS 的事件选择移入二级菜单，支持 Enter / Space 连续勾选并保存，Esc 返回；两套选择独立保留。
- 历史消息的原始业务载荷改为单份保存，自动迁移旧存储，保持分页、去重及删除语义。
- 切换不同账号（含扫码登录）和删除当前账号须先确认并停止旧直播及关联 OBS；失败时保留当前账号，不受退出设置影响。
- 将账号与直播编排从 TUI 分离到应用层，统一客户端空闲连接回收，并保留 API 与封面响应读取过程中的取消原因。
- 在 Linux、macOS、Windows 运行基本核心测试；Make、CI 与六平台发行共用构建入口，独立 TUI 保持无 CGO 构建，Nix 保持声明式及一致的配套产物布局。
- 每周检查默认构建与原生语音标签依赖漏洞；发行保留 SHA-256 校验和并增加 GitHub 构建来源证明。过时的 TTS 开发规划移至本地归档。

- 封面使用字符预览时显示黄色提示，说明图形预览不可用并推荐支持 Kitty 图形协议的终端；窄窗口自动换行。
- 浮层默认左侧居中、零偏移、字号 16，内容默认为实时弹幕并排在内容选项首位；已有保存值保持不变。

- 消息按时间从上到下排列；跟随时自动滚到底部，「显示其他事件」开关固定在顶部。
- 入场和关注默认显示，分享和特别关注需开启 `f`，互关通知隐藏。当前支持的 30 个 PK 命令均需开启 `f`。
- 隐藏跨房广播、推荐、下播列表和非 PK 榜单等通知，即使开启 `f` 也不会显示；原始业务消息仍保存。
- 历史、原始业务消息及操作日志保留最近七天并自动清理；有可靠标识的消息支持跨重启去重。
- OBS 连接设置集中到独立的「OBS」页，代理和推流协议保留在「设置」页。
- 补充界面截图，精简中英文 README，并将逐项白名单移至 `docs/`。
- 分开聊天状态管理和显示逻辑，统一切房状态重置，删除冗余代码和重复测试。

### 修复

- 清除应用数据时一并删除仍由当前账号 ID 引用的凭据，避免账号索引不完整时遗留登录信息。
- 恢复默认设置时回收旧客户端的空闲连接，并回收退出检查使用的临时连接。
- 修复极窄终端中封面降级提示的双宽字符越界问题。
- 按主程序真实路径定位 `libexec` 中的浮层，支持整体搬迁和通过符号链接启动，不依赖当前工作目录。
- 移除「恢复默认外观」后的多余冒号，增加操作确认和恢复成功提示。
- 修复滚动弹幕时意外暂停跟随的问题，确保新消息及时显示。
- 修复暂停、翻页及异步读取时的阅读位置问题；隐藏通知不再触发新消息提示，提醒不会串到其他房间。
- 修复从历史房间返回直播房间后立即暂停时可能误用旧房间序号的问题。
- 切换账号时立即隐藏旧弹幕监听来源，防止浮层接收旧房间的异步结果。
- 拒绝超出 protobuf 合法范围的字段号，同时兼容合法的未知字段。
- 状态栏长错误信息按终端宽度自动换行，并为多行提示预留显示高度，避免错误详情被截断。
- 退出时不再仅因启用自动推流而尝试连接未使用的 OBS；保留开播及已知推流活动的停止检查，并精简连接失败提示。

### 注意事项

- 历史数据库会自动升级为带版本的新格式。降级应用前请恢复升级前的数据库备份，不要让旧版本直接打开已升级的数据库。
- 断线期间的消息不会在重连后补回；本地历史可能不完整，不能作为完整财务对账依据。
- 能解析的消息不一定展示；未列入白名单或解析失败的消息仍隐藏。

## [v0.2.0-alpha]：2026-09-16（预发布）

### 新增

- 登录后自动监听自己的直播间，支持弹幕、礼物、醒目留言（SC）和大航海消息；可浏览本地历史、暂停跟随、分页及切换房间。
- 解析 177 个已注册命令，支持新版礼物、互动和高能榜的 protobuf 消息格式。
- 显示白名单区分常态显示、开启 `f` 后显示和始终隐藏的消息，完整清单见[弹幕显示白名单](docs/danmaku-whitelist.md)。
- 暂停或翻阅历史时，在顶部提示有新消息；按 `Space` 或 `End` 返回最新。
- 提供 Nix Flake 构建、运行入口和检查任务，并锁定构建依赖。
- 使用 `nix develop`（或 `nix develop .#arcana-world`）进入独立的 TUI 开发环境，使用 `nix develop .#arcana-overlay` 进入浮层开发环境；Linux 下会自动启用 Wayland 构建标签。开发使用 Go 模块模式，无需生成 `vendor/`。
- 在设置页管理原生浮层，支持运行时启停、外观持久化及房间状态、实时弹幕、组合三种内容模式；命令行可临时覆盖启用状态。
- 提供 `make desktop` 和 Nix 桌面配套包；跨平台发行统一为根目录主程序与 `libexec` 浮层组成的便携包，保留独立 TUI 开发构建。
- 设置页底部新增「恢复默认设置」和「清除应用数据」。恢复默认仅影响设置页配置，保留 OBS 配置和登录凭据；清除须输入 `arcanaworldclear`，关闭后台资源后删除当前数据目录的应用文件及对应登录凭据并退出，输入不匹配则返回设置页。

### 改进

- 封面使用字符预览时显示黄色提示，说明图形预览不可用并推荐支持 Kitty 图形协议的终端；窄窗口自动换行。
- 浮层默认左侧居中、零偏移、字号 16，内容默认为实时弹幕并排在内容选项首位；已有保存值保持不变。

- 消息按时间从上到下排列；跟随时自动滚到底部，「显示其他事件」开关固定在顶部。
- 入场和关注默认显示，分享和特别关注需开启 `f`，互关通知隐藏。当前支持的 30 个 PK 命令均需开启 `f`。
- 隐藏跨房广播、推荐、下播列表和非 PK 榜单等通知，即使开启 `f` 也不会显示；原始业务消息仍保存。
- 历史、原始业务消息及操作日志保留最近七天并自动清理；有可靠标识的消息支持跨重启去重。
- OBS 连接设置集中到独立的「OBS」页，代理和推流协议保留在「设置」页。
- 补充界面截图，精简中英文 README，并将逐项白名单移至 `docs/`。
- 分开聊天状态管理和显示逻辑，统一切房状态重置，删除冗余代码和重复测试。

### 修复

- 清除应用数据时一并删除仍由当前账号 ID 引用的凭据，避免账号索引不完整时遗留登录信息。
- 恢复默认设置时回收旧客户端的空闲连接，并回收退出检查使用的临时连接。
- 修复极窄终端中封面降级提示的双宽字符越界问题。
- 按主程序真实路径定位 `libexec` 中的浮层，支持整体搬迁和通过符号链接启动，不依赖当前工作目录。
- 移除「恢复默认外观」后的多余冒号，增加操作确认和恢复成功提示。
- 修复滚动弹幕时意外暂停跟随的问题，确保新消息及时显示。
- 修复暂停、翻页及异步读取时的阅读位置问题；隐藏通知不再触发新消息提示，提醒不会串到其他房间。
- 修复从历史房间返回直播房间后立即暂停时可能误用旧房间序号的问题。
- 切换账号时立即隐藏旧弹幕监听来源，防止浮层接收旧房间的异步结果。
- 拒绝超出 protobuf 合法范围的字段号，同时兼容合法的未知字段。
- 状态栏长错误信息按终端宽度自动换行，并为多行提示预留显示高度，避免错误详情被截断。
- 退出时不再仅因启用自动推流而尝试连接未使用的 OBS；保留开播及已知推流活动的停止检查，并精简连接失败提示。

### 注意事项

- 断线期间的消息不会在重连后补回；本地历史可能不完整，不能作为完整财务对账依据。
- 能解析的消息不一定展示；未列入白名单或解析失败的消息仍隐藏。

## [v0.1.4]：2026-09-14

### 新增

- 提供中英文界面与提示，支持自动识别系统语言及通过 `--lang` 手动切换。
- 提供 macOS、Windows、Linux 的 x64 / ARM64 发布包，共六种平台组合。
- 提供中英文 README、项目图标、安装链接、OBS 配置说明和快捷键文档。

### 改进

- 将版本发布从常规 CI 拆分为独立工作流，校验版本标签后再构建发布包。
- 发布包统一附带许可证和 SHA-256 校验文件，支持正式版和预发布版本。

## [v0.1.3]：2026-09-13

### 新增

- 提供终端直播控制台，支持扫码登录、多账号保存与切换，以及开播和停播。
- 支持修改直播间标题、分区、公告及封面，并提供封面裁剪和终端预览。
- 支持 OBS WebSocket v5 连接、写入推流配置，以及自动同步开播和停播。
- 支持 RTMP / SRT 和网络代理；账号凭据与 OBS 密码通过系统密钥环保存。
- 提供持久化配置、操作日志，以及 `--config-dir`、`--version` 等命令行选项。
- 建立基础测试和 CI，提供 Linux x64 发布包及校验文件。

[v0.3.0-alpha.1]: https://github.com/TarocchiLive/arcana-world/releases/tag/v0.3.0-alpha.1
[v0.2.1]: https://github.com/TarocchiLive/arcana-world/releases/tag/v0.2.1
[v0.2.0]: https://github.com/TarocchiLive/arcana-world/releases/tag/v0.2.0
[v0.2.0-rc.2]: https://github.com/TarocchiLive/arcana-world/releases/tag/v0.2.0-rc.2
[v0.2.0-rc.1]: https://github.com/TarocchiLive/arcana-world/releases/tag/v0.2.0-rc.1
[v0.2.0-alpha]: https://github.com/TarocchiLive/arcana-world/releases/tag/v0.2.0-alpha
[v0.1.4]: https://github.com/TarocchiLive/arcana-world/releases/tag/v0.1.4
[v0.1.3]: https://github.com/TarocchiLive/arcana-world/releases/tag/v0.1.3
[v0.2.2]: https://github.com/TarocchiLive/arcana-world/releases/tag/v0.2.2
