# 更新日志

**中文** · [English](CHANGELOG.en.md)

按版本倒序记录主要变化。v0.1.5 暂记为下一版本，尚未发布；发版时补充日期，再建立下一版本的记录。

## [v0.1.5] — 未发布

### 新增

- **弹幕与历史**：登录后自动监听自己的直播间，支持弹幕、礼物、醒目留言（SC）和大航海消息；提供本地历史、暂停跟随、分页及房间切换。
- **事件解析**：覆盖 177 个已注册命令，支持新版礼物、互动和高能榜的 protobuf 消息格式。
- **显示白名单**：区分常态显示、开启 `f` 后显示和始终隐藏的消息，完整清单见[弹幕显示白名单](docs/danmaku-whitelist.md)。
- **新消息提醒**：暂停或翻阅历史时，在顶部提示有新消息；按 `Space` 或 `End` 返回最新。
- **Nix 支持**：增加 Flake 构建、运行入口与检查任务，锁定构建依赖。
- **Nix 开发环境**：`nix develop`（或 `nix develop .#arcana-world`）进入独立的 TUI 开发环境；`nix develop .#arcana-overlay` 提供浮层开发依赖，Linux 下自动启用 Wayland 构建标签。开发使用 Go 模块模式，无需生成 `vendor/`。

### 改进

- 消息按时间从上向下排列，跟随时自动滚到底部；「显示其他事件」开关固定在顶部。
- 入场、关注常态显示；分享、特别关注随 `f` 显示；互关通知隐藏。当前支持的 30 个 PK 命令均归入 `f`。
- 隐藏跨房广播、推荐、下播列表、非 PK 榜单等通知，开启 `f` 也不会放开；原始业务消息仍保存。
- 历史、原始业务消息及操作日志保留最近七天，自动清理过期记录；有可靠标识的消息支持跨重启去重。
- OBS 连接设置集中到独立的「OBS」页，代理与推流协议保留在「设置」页。
- 补充界面截图，精简中英文 README，并将逐项白名单移至 `docs/`。
- 分离聊天状态与显示逻辑，统一切房状态重置，精简冗余代码和重复测试。

### 修复

- 修复滚动弹幕时隐式暂停跟随、导致新消息不再及时显示的问题。
- 修复暂停、翻页及异步读取时的阅读位置问题；隐藏通知不再触发新消息提示，提醒不会串到其他房间。
- 修复从历史房间返回直播房间后立即暂停时，可能误用旧房间序号的问题。
- 拒绝超出 protobuf 合法范围的字段号，继续兼容合法的未知字段。

### 注意事项

- 断线期间的消息不会在重连后补回；本地历史不保证完整，不能作为完整财务对账依据。
- 支持解析不等于默认展示；未列入白名单或解析失败的消息仍隐藏。

## [v0.1.4] — 2026-09-14

### 新增

- 增加中英文界面与提示，支持自动识别系统语言及通过 `--lang` 手动切换。
- 增加 macOS、Windows、Linux 的 x64 / ARM64 发布包，共六种平台组合。
- 增加中英文 README、项目图标、安装链接、OBS 配置说明与快捷键文档。

### 改进

- 将版本发布从常规 CI 拆分为独立工作流，校验版本标签并在检查通过后构建发布包。
- 发布包统一附带许可证和 SHA-256 校验文件，支持正式版与预发布版本的发布流程。

## [v0.1.3] — 2026-09-13

### 新增

- 提供终端直播控制台：扫码登录、多账号保存与切换、开播和停播。
- 支持修改直播间标题、分区、公告及封面，提供封面裁剪与终端预览。
- 支持 OBS WebSocket v5 连接、推流配置写入及自动同步开停播。
- 支持 RTMP / SRT 和网络代理；账号凭据与 OBS 密码通过系统密钥环保存。
- 增加持久化配置、操作日志，以及 `--config-dir`、`--version` 等命令行选项。
- 建立基础测试与 CI，提供 Linux x64 发布包及校验文件。

[v0.1.5]: https://github.com/TarocchiLive/arcana-world/tree/dev
[v0.1.4]: https://github.com/TarocchiLive/arcana-world/releases/tag/v0.1.4
[v0.1.3]: https://github.com/TarocchiLive/arcana-world/releases/tag/v0.1.3
