# 项目协作要求

- 代码注释必须使用中文。
- 复用现有配置、命名和错误处理；平台差异放在对应后端，禁止无用兼容别名和假实现。
- Go TUI 不得强制依赖其他二进制执行模块。
- 只保留基本核心测试，不做大部分单元测试和集成测试。
- 用户文案简短直接，只说明操作、结果和必要风险，不展示无关实现细节。
- README 只保留用户说明；仅按用户要求或为纠正过时说明作必要修改，不写开发记录、验证报告或更新日志。
- 命令行用法放在 `--help`，新增用法同步更新`--help`文案，项目来源放在 README 致谢；新增文档须有明确用途。
- 开发记录和临时程序放在 `.local/`，不得提交，完成后清理无用产物。
- 所有提交（含合并、发布）使用英文 `type(scope): subject`，scope 可省略，填写时使用小写模块名。
- 提交 type 仅限 `feat`、`fix`、`docs`、`refactor`、`perf`、`style`、`test`、`build`、`ci`、`chore`、`revert`、`release`、`merge`。
- 破坏性提交在更新日志冒号前加 `!`，正文写明 `BREAKING CHANGE:`。
- 提交前核对暂存内容和完整提交信息；历史重写前备份并明确分支、标签范围；未经明确授权不得强制推送。