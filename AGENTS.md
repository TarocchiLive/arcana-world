# 项目协作约定

## 文档与临时记录

- README.md 和 README.en.md 面向最终用户，不是开发草稿、实现笔记、验证报告或更新日志。
- 不要因为完成了代码变更，就自动把变更摘要、内部设计、调试过程、测试结果或待办堆进 README。
- 仅在用户明确要求，或已有用户使用说明因变更变得错误时，对 README 做必要的最小修改；不要借机扩写。
- 需要保存的开发计划、调查结论、验证记录、临时脚本和内部 TODO 放在项目根目录 `.local/` 中。该目录由 `.gitignore` 排除，不得提交。
- 新增面向用户的文档必须有明确用途，不要创建无人维护的说明文件。

## 用户文案（重要规范）

- 面向用户的文案必须以操作和直接结果为中心，不默认附带实现说明；信息完整不等于把所有技术细节都展示给用户。
- 页面介绍、状态提示和命令行帮助应简短直接。选项说明写清用途和可选值即可，不堆叠括号、分号、附注或免责声明。
- 不主动展示后端选择、回退顺序、元信息落盘、迁移策略等内部机制，也不添加“诊断结果为英文”这类不影响操作的提示。
- 只有用户当下需要据此作决定的信息，才在对应操作处提示；必要的风险或失败信息应准确表达，不在无关页面反复说明。
- 说明放在对应位置：命令行用法放 `--help`，不要自动扩写 README；项目来源保留在 README 致谢中，不反复出现在运行界面。
- 用户要求删除文案时，只修改表述，不擅自改变相关功能行为；中英文文案同步调整。

## 实现与验证

- 保持现有 Go TUI 的独立性。可选桌面浮层不得让 TUI 强制依赖图形运行时。
- 复用现有配置、命名和错误处理方式；平台差异限制在对应后端中，不引入无用的兼容别名或假实现。
- 浮层必须鼠标穿透、不抢焦点，按完整显示器边界定位，不随前台窗口或工作区避让而移动。动态内容更新不得隐式改变位置。
- 平台不支持或缺少构建依赖时明确报错，不静默回退为无法满足行为约定的普通窗口。
- 修改图形行为后实际运行对应后端验证；交叉编译不等于运行验证，模拟环境不等于真机。交付时明确区分已验证和未验证的场景。
- 平台验证用临时程序放 `.local/`，完成后移除不再需要的产物。
- 如非必要，勿增测试，项目始终只保留最基本测试。

## Git 操作（硬约束）

- 所有提交必须使用英文 Conventional Commits 格式：`type(scope): subject`。`scope` 可省略；破坏性变更在冒号前加 `!`，并在正文中写明 `BREAKING CHANGE:`。
- `type` 仅允许：`feat`、`fix`、`docs`、`refactor`、`perf`、`style`、`test`、`build`、`ci`、`chore`、`revert`、`release`、`merge`。按实际改动选择类型；`scope` 使用小写模块名，例如 `overlay`、`tui`、`tts`、`store`。
- `subject` 必须以小写英文动词开头，使用祈使语气描述具体改动；标题总长度不超过 72 个字符，末尾不加句号。禁止无类型前缀的 `Add ...`、`Fix ...`、`Update ...`，以及 `init commit`、`update` 等含糊标题。
- 正文如有，必须使用英文，与标题空一行，说明必要的原因和行为变化；不要粘贴实现流水账、工具输出或整份验证报告。
- 合并和发布提交同样遵守格式；禁止直接使用 Git 生成的 `Merge branch ...` 等默认提交信息。
- 每次提交前必须核对暂存内容与完整提交信息；不符合以上规则时先修正，不得提交。历史重写必须先备份，明确分支和标签范围；未经用户明确授权，不得强制推送。