# 可选 TTS 功能：上下文、实测与接入建议

本文归档本次 TTS 调研和本地实验，供后续开发接续使用。它是规划记录，不代表 Arcana World 已实现语音播报、模型下载或弹幕接收。

## 需求与当前决策

用户希望在 Linux 上获得可接受的中文弹幕播报效果，试听 eSpeak 后明确认为音质不可接受。随后讨论并实际测试了本地 Kokoro，由 Go API 调用，关注延迟、CPU、内存及 GPU 占用。最终关心的问题是：能否将其作为当前 Go TUI 项目的可选功能，由用户在 TUI 内下载模型并启用。

结论：可以实现。推荐主 TUI 保持现有构建方式，按需安装独立语音组件和模型，由 TUI 管理组件生命周期。用户不必安装 Python、手动运行 API 或配置后台服务。

截至本记录：

- 尚未决定正式实现，也未修改项目运行代码或加入推理依赖。
- 推荐独立子进程方案，但这仍是设计建议，不是用户已确认的最终接口合同。
- 当前 M2 Pro 上推荐优先考虑 FP32 模型 + CPU 双线程；不能直接将该选择推广到 Linux。
- CoreML 已实际尝试，没有得到加速收益，不能宣称 GPU 加速成功。
- 本分支只归档规划。创建分支时已有的 README 修改和四张截图不是本次 TTS 工作，未纳入本次提交。

## 候选语音方案

### eSpeak / eSpeak NG

用户已经试听并否定音质，不再作为中文弹幕播报的推荐方案。部分神经语音模型使用 `espeak-ng-data` 进行发音处理，不等于最终输出仍是 eSpeak 的合成声音。

### edge-tts

第三方 Python 客户端，访问微软 Edge 在线语音服务；不需要安装 Edge 浏览器。可使用中文声音，例如 `zh-CN-XiaoxiaoNeural`、`zh-CN-YunxiNeural`，当前可用列表应以服务实际返回为准。

优点：无需本地模型，本机推理负担小。限制：必须联网，文本发送至微软；不是具有可用性承诺的正式云 API，存在接口变化和服务限制风险。Linux 上其 `edge-playback` 命令需要 mpv。

本次没有对 edge-tts 做本机性能测试，也没有将其集成到项目。它可以作为后续在线方案候选，不应因为本次做了 Kokoro 实验就自动扩大实现范围。

### Kokoro + sherpa-onnx

Kokoro 是约 8200 万参数的神经语音模型。sherpa-onnx 提供 Go 绑定，实际调用原生推理库及 ONNX Runtime，不是纯 Go 推理。

本次使用 `kokoro-multi-lang-v1_1` 和 `kokoro-int8-multi-lang-v1_1`。该版本支持中英文，103 个音色，输出采样率 24 kHz。

音色编号：

- 0–1：美国英语女声；2：英国英语女声。
- 3–57：中文女声；58–102：中文男声。
- 本次统一使用 `sid=3`，即 `zf_001`，语速 1。

不要误将 `sid=0` 当作该版本的默认中文女声。不同模型版本的编号也不能混用。

## 实验环境与方法

### 环境

- 本机 macOS，darwin/arm64，Apple M2 Pro。
- 12 个逻辑 CPU，32 GiB 内存；不是 Linux 测试。
- Go 1.27.1。
- `github.com/k2-fsa/sherpa-onnx-go/sherpa_onnx` v1.13.8。
- 默认 CPU 双线程；另试单线程及 CoreML。
- 模型常驻，一个模型实例；通过互斥锁串行合成。
- HTTP 接口整句合成完成后返回 WAV，不是流式首音频输出。
- 性能数据不包含播放器启动、音频设备缓冲或实际听到声音的延迟。
- 在用户正在使用的机器上测试，未控制整机其他负载；结果是冒烟测量，不是严格基准。

### 实验接口

在项目外的临时目录编写了 Go HTTP API，没有修改 Arcana World 的 go.mod：

- `GET /health`：进程 PID、累计 CPU 时间、最高 RSS、模型加载时间、线程数、音色数等。
- `POST /tts`：接收 `{"text":"欢迎来到直播间，感谢关注。","sid":3}`，返回 WAV。
- 返回头：`X-Synthesis-Seconds`、`X-Audio-Seconds`。
- 仅监听 localhost。原 CPU 实例端口 18765，CoreML/FP32 对照实例端口 18766。
- 原接口限制输入大小、文本长度和音色范围，但仍是实验代码，不是生产级服务实现。
- 后续版本将健康状态字段标记为 `requested_provider`，避免把请求的后端名称当作实际硬件执行证明。

音频检查：解析 WAV、检查单声道/16-bit/24 kHz，以及 PCM 的 RMS 大于 10，拦截全零静音。该检查证明生成了非静音音频，不证明中文发音完全正确或音质令人满意。

CPU 测量采用 `getrusage` 进程累计 user + system CPU 时间差除以墙钟时间。100% 表示一个核，不表示整机满载；不能用瞬时 ps 百分比代替空闲期间 CPU 时间差。

内存测量采用整个进程 RSS，而不是 Go 堆：CPU 初轮每 200 ms 采样，后续 CoreML 对照约每 250–500 ms 采样，并参考最高 RSS。RSS 包括原生推理库；峰值与句长、请求数量、执行路径有关。

### 临时文件

本次机器上的目录：`/tmp/kokoro-go-smoke.GmELK5/`。

其中包含 main.go、go.mod/go.sum、构建后的实验二进制、smoke.py、模型包、生成 WAV，以及以下报告：

- `report.json`
- `threads2-results.json`
- `threads1-failure.json`
- `gpu-results.json`
- `coreml-int8-results.json`
- `coreml-fp32-results.json`
- `cpu-fp32-results.json`
- `coreml-comparison.json`

这些临时文件和模型未纳入仓库，可能被系统清理；本文保留关键方法和结果。上述路径、PID、运行端口不构成长期部署保证。额外 CoreML/FP32 实验进程已停止，最后检查时原 CPU 接口仍可用。

## CPU 双线程 INT8 实测

模型及配套文件解压后的总大小约 205.35 MiB，压缩包约 140.22 MiB。模型本体约 109 MB，音色文件约 51.3 MB，另有词典、规则和发音数据；安装容量不能只算 ONNX 文件。

- 模型初始化：0.81 秒，不包含下载，未清空操作系统文件缓存。
- 加载后空闲 RSS：约 436 MiB。
- 本轮峰值 RSS：约 579 MiB。
- 最后空闲 10 秒 RSS：仍约 579 MiB，没有立即回落；短测试不足以判断长期泄漏或内存上限。
- 连续生成平均 CPU：153.88%，约 1.54 个核。
- 初始空闲 CPU：约 0.006%；生成后空闲 CPU：约 0.075%。
- 首次请求“欢迎来到直播间，感谢关注。”：HTTP 2.675 秒，生成音频 2.979 秒。

下面每条重复三次取平均，字数包含标点和英文字符：

| 文本 | 字符数 | HTTP 耗时 | 音频时长 |
| --- | ---: | ---: | ---: |
| 谢谢关注。 | 5 | 1.154 秒 | 1.259 秒 |
| 欢迎来到直播间，感谢大家的支持。 | 16 | 2.970 秒 | 3.585 秒 |
| 今天我们测试本地中文语音播报，看看生成速度怎么样。 | 25 | 3.966 秒 | 5.296 秒 |
| 主播晚上好，请问这个软件可以在 Linux 上运行吗？ | 27 | 4.333 秒 | 5.227 秒 |
| 感谢送来的礼物，祝大家周末愉快。 | 16 | 2.861 秒 | 3.427 秒 |
| 现在是晚上八点三十分，我们准备开始今天的直播。 | 23 | 3.899 秒 | 5.146 秒 |

18 条连续请求耗时约 57.62 秒，HTTP 耗时中位数 3.434 秒，最大 4.381 秒。累计 RTF 约 0.801，即约 1.25 倍实时生成速度。RTF = 生成时间 / 音频时长，越小越快；比实时快不等于立即出声。

另测三个同时到达的请求。由于模型实例串行合成，请求出现排队，最慢约 8.05 秒。总计完成 22 次有效请求：1 次首次请求、18 次连续请求、3 次同时到达的请求。该并发测量不是多模型吞吐测试。

### 单线程异常

同一 INT8 模型设置一个推理线程后，在两次独立进程启动中均出现全零静音音频，冒烟检查失败。

诊断请求“欢迎来到直播间，感谢大家的支持。”：接口报告合成 3.479 秒，音频时长 5.2 秒，但 124800 个 PCM 样本最小值、最大值、RMS 均为 0。恢复双线程后，同一文本输出有效非静音音频，RMS 约 2374.75。

未定位根因，不能断定是 Kokoro 普遍缺陷，也不能用静音输出时间作为有效性能数据。本次没有验证 FP32 单线程。

## GPU 与 CoreML 实验

### 原 CPU 模式的 GPU 观察

使用无需提权的 IORegistry AGXAccelerator 数据，每约 0.5 秒采样：空闲 10 秒、连续合成 8 条约 32 秒、再空闲 5 秒。

| 阶段 | 整机 GPU 平均利用率 | 采样次数 | API 进程 GPU 客户端 |
| --- | ---: | ---: | ---: |
| 合成前空闲 | 23.3% | 19 | 0 |
| 连续合成 | 22.4% | 60 | 0 |
| 合成后空闲 | 22.4% | 10 | 0 |

这是“未检测到进程 GPU 使用”，不是读取进程利用率计数器测得精确的 0.00%。没有客户端时，将其记录中的 GPU 时间求和得到 0，也不能当作独立的硬件计数器证据。整机原有 GPU 负载不能归因于 Kokoro。

`powermetrics` 需要管理员权限；普通调用失败，`sudo -n` 也因需要密码而失败。本次未获取该权限，未精确测量进程 GPU 利用率、GPU 功耗或神经网络引擎占用。

### 实际启用 CoreML

不止查看配置名：通过 sherpa-onnx 的 `coreml` 后端运行真实合成，并使用 `coreml:coreml.config`、配置文件 `LogSeverityLevel=0` 查看 ONNX Runtime 节点分配。

- INT8 原图 3161 个节点，CoreML 支持 1486 个，分成 144 个子图；分图后日志列出 144 个 CoreML 节点和 1691 个 CPU 节点。
- FP32 原图 2386 个节点，CoreML 支持 1218 个，分成 115 个子图；分图后日志列出 115 个 CoreML 节点和 1184 个 CPU 节点。
- 原图节点与分图后节点计数口径不同，不能直接混加计算占比。
- CoreML 后端确实启用并接管部分计算，但不代表这些计算必定由 GPU 执行。
- 两组采样均未发现 API 进程的 GPU 客户端。CoreML 的具体硬件选择、可能存在的独立执行服务以及 ANE 使用未被证实，不能宣称 GPU 加速成功或所有 CoreML 工作必定在 CPU 执行。

### 同模型性能对照

每组新增实验使用两条文本各三次，共六次有效请求。25 字句子为“今天我们测试本地中文语音播报，看看生成速度怎么样。”，生成约 5.3 秒音频。

| 模型 / 后端 | 25 字平均生成耗时 | 连续生成平均 CPU | 本轮峰值 RSS |
| --- | ---: | ---: | ---: |
| INT8 / CPU，上一轮 | 3.97 秒 | 154% | 579 MiB |
| INT8 / CoreML | 4.15 秒 | 153% | 913 MiB |
| FP32 / CPU | 2.46 秒 | 198% | 794 MiB |
| FP32 / CoreML | 2.54 秒 | 198% | 1213 MiB |

原 CPU INT8 组使用 22 次请求且表中为 HTTP 耗时，其余组使用六次请求且表中为服务端合成耗时；本机 HTTP 开销很小，但两种口径仍应注明。RSS 峰值不是同等持续时长的严格内存基准。

FP32 CPU 的 13 字句子“欢迎来到直播间，感谢关注。”预热后约 1.62 秒；CoreML 约 1.70 秒。

结论：

1. 这台 M2 Pro 上，INT8 更小，但比 FP32 慢；不要以“量化”推断“更快”。
2. FP32 的速度改善来自模型/执行路径变化，不能归为 GPU 收益；同模型 CPU 比 CoreML 略快。
3. CoreML 增加内存占用，本次没有实际性能收益，不推荐作为首选。
4. 当前机器优先考虑 FP32 + CPU 双线程，代价是生成时接近两个核、约 0.8 GiB RSS。
5. Linux 的 NVIDIA CUDA、AMD/Intel 后端未测，不应套用 Mac 结论。CUDA 需要匹配的 GPU 推理运行库；本次没有验证对应发布包。

## 当前项目的接入成本

### 已核实的工程约束

- 主项目 go.mod 声明 Go 1.26，TUI 使用 Bubble Tea v1.3.10。
- `.github/workflows/release.yml` 设置 `CGO_ENABLED=0`，发布 macOS/Windows/Linux 的 amd64/arm64 包。
- `flake.nix` 同样设置 `CGO_ENABLED=0`。
- sherpa-onnx 的 macOS arm64 Go 绑定使用 `import "C"`，链接 `libsherpa-onnx-c-api` 和 `libonnxruntime`，构建参数含指向 Go 模块缓存的 rpath。
- 实验程序能在开发机器上加载模块缓存中的库，不代表拷贝到其他机器后可运行。正式组件必须自带匹配库并修正相对加载路径，验证可重定位性。
- 当前 `internal/tui/model.go` 的 `Model.work()` 设置全局 `m.busy`，并使用 60 秒超时，不适合直接承载大模型下载。
- 项目已有 OBS 独立事件处理方式，可沿用异步消息驱动思路，不应在 TUI 主更新循环执行阻塞下载、模型初始化或合成。
- 本次检查的当前代码未发现弹幕接收实现。如果目标是自动读弹幕，弹幕连接/事件接收必须另行纳入范围，不能把 TTS 安装当成完整弹幕功能。

### 直接链接进主程序

优点是调用简单、少一层进程通信，但必须引入 CGO、原生库打包和跨平台编译链，并修改现有发布及 Nix 构建方式。功能默认关闭也无法消除编译和动态链接依赖。原生崩溃会影响主 TUI，原生内存生命周期也更难与主进程隔离。

不推荐为了一个可选功能直接改变现有主程序的发行方式。

### 推荐：TUI 管理可选语音子进程

主 TUI 保持当前构建方式；另发布包含 Go worker、sherpa-onnx、ONNX Runtime 及所需音频组件的语音包。用户选择启用时按需安装并启动。

建议的边界：

- 主 TUI：安装/下载进度、配置、待播报文本策略、用户操作和状态展示。
- 语音组件：加载模型、串行合成、音频输出、取消/停止处理和释放原生资源。
- 同机通信优先 stdin/stdout，不必照搬实验 HTTP API；控制协议带请求 ID、版本和状态/错误消息，日志走 stderr，防止污染协议。
- 若由 worker 播放，不必把大段 WAV 再传回主进程。跨平台播放库选型仍需验证。
- 实验暂用互斥锁保证模型串行调用，正式版本需要有界队列，防止无限积压。停止当前语音、清空队列、队列满时的处理必须明确定义；不能擅自决定用户想丢弃哪些弹幕。
- 默认不安装、不启用。首次启用明确展示下载内容及大小；用户不需手动启动额外服务。
- 关闭语音或退出 TUI 时停止子进程，可靠释放原生模型内存。不应每条弹幕都重新启动/加载模型。
- worker 崩溃要显示可操作错误，不自动启动无限重试循环，不阻断开播/停播/OBS 操作。

该方案属于中等规模的可选功能，而不是添加一个 import 和开关。复杂度主要在多平台分发、音频输出及下载/进程生命周期管理。

## TUI 内安装与启用的建议流程

界面流程：设置 → 语音播报 → 未安装 → 下载并安装 → 校验 → 安装完成 → 选择音色 → 试听 → 启用。

实现要求：

1. 按 OS/架构选择已验证的组件版本；不支持的平台明确提示，不静默回退至不兼容二进制。
2. 将组件/模型版本、下载地址、大小、SHA-256 和兼容性关系放入可信发布清单。下载可执行文件属于软件更新安全边界。
3. 异步下载，展示字节进度、状态、失败原因并支持取消；不占用主界面全局 busy，不直接套用 60 秒通用任务超时。
4. 下载到临时文件，校验通过后原子安装；失败或取消不能把半包标成可用。若解压归档，必须限制路径穿越、危险链接和解压规模。
5. 放在用户可写数据目录，不写程序安装目录。路径遵循项目配置习惯和平台数据目录规范；配置中不保存易变化的临时路径。
6. 已下载但未启用只占磁盘，不启动推理组件；启用后再常驻模型。
7. 状态可区分未安装、下载中、校验中、已安装、加载中、可用、失败，不只显示一个布尔开关。
8. 下载成功不等于可用：检查模型/组件版本后实际加载并通过试听路径；将库缺失、模型错误、设备不可用等问题分别显示。
9. 模型删除、版本更换和组件更新不能与正在运行的 worker 争用文件；原子切换和停止顺序必须明确。
10. 用户可以卸载模型/组件以释放空间。更新、自动重试、复杂后端抽象不应在没有需求时顺带扩张。

## 音频与 Linux/Nix 注意事项

本次验证的是文本到 WAV。macOS 播放示例用了 `afplay`，这不是跨平台播放实现。若要做到下载后直接可用，不能默认 Linux 用户安装了 mpv/ffplay；组件需提供经验证的音频输出方式，处理设备不可用和停止播放。

Linux 需要实际验证目标音频栈、动态库加载、最低 glibc/系统要求；macOS 要验证原生库重定位及分发限制，Windows 要验证 DLL 搜索路径和音频设备。amd64/arm64 支持不能只从上游包名推断完成。

NixOS 不能假设下载一个普通 Linux 动态链接二进制就能运行。应提供声明式打包语音组件/依赖的路径；模型可按需放用户数据目录，运行时模型下载不等于让 Nix 构建联网。现有 README 的 Nix 段落已由用户删除，不应未经请求重新加入。

若以后需要让 OBS 捕获播报声音，应单独明确音频路由需求；播放到系统默认设备不等于已正确混入直播音轨。

## 后续实施前需要验证的事项

- 在真实目标 Linux CPU 上比较 INT8 和 FP32，不能以 M2 Pro 数据决定默认值。
- 确认要做的是通用 TTS/试听，还是完整弹幕接收与自动播报。
- 验证 worker 在干净环境的可重定位发行包，不依赖开发者 Go 模块缓存。
- 选择并验证跨平台播放实现，包含取消/停止、设备错误与退出清理。
- 明确队列溢出和过期弹幕处理策略；当前实验不代表最终语义。
- 在真实 TUI 中验证下载期间仍可操作直播与 OBS，测试取消后重装、校验失败、worker 崩溃、退出释放资源。
- 若继续追求 GPU，应将实际硬件执行与同模型 CPU 性能对照作为验收，不接受仅显示 provider=coreml/cuda。
- 审核实际再分发的模型、音色、发音数据和原生库许可证及告知要求，不把模型的许可自动套到全部依赖。

## 来源

- edge-tts：https://github.com/rany2/edge-tts
- Kokoro v1.1 中文模型：https://huggingface.co/hexgrad/Kokoro-82M-v1.1-zh
- sherpa-onnx Kokoro 模型说明：https://k2-fsa.github.io/sherpa/onnx/tts/pretrained_models/kokoro.html
- v1.1 音色编号与试听：https://k2-fsa.github.io/sherpa/onnx/tts/all/Chinese-English/kokoro-multi-lang-v1_1.html
- Go 绑定：https://github.com/k2-fsa/sherpa-onnx-go
- Go TTS 示例：https://github.com/k2-fsa/sherpa-onnx/tree/v1.13.8/go-api-examples/non-streaming-tts
- 固定版本后端选择实现：https://github.com/k2-fsa/sherpa-onnx/blob/v1.13.8/sherpa-onnx/csrc/session.cc
- INT8 模型包：https://github.com/k2-fsa/sherpa-onnx/releases/download/tts-models/kokoro-int8-multi-lang-v1_1.tar.bz2
- FP32 模型包：https://github.com/k2-fsa/sherpa-onnx/releases/download/tts-models/kokoro-multi-lang-v1_1.tar.bz2
